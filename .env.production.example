# Copy to .env.production.local and fill in. That file is gitignored.
# Used by scripts/deploy-cloudflare.sh to set the Cloudflare Worker secrets.

# Neon pooled connection string
DATABASE_URL=""

# openssl rand -base64 48
AUTH_SECRET=""

ADMIN_EMAIL=""
ADMIN_PASSWORD=""

# Optional — without these, emails are only written to the Worker log
RESEND_API_KEY=""
MAIL_FROM="RootSym <onboarding@resend.dev>"
ADMIN_NOTIFY_EMAIL=""
